# مهمة AGY — استبدال واجهة Spark بواجهة ATHREDU

## النتيجة المطلوبة

استبدل واجهة وتجربة مستخدم تطبيق **Spark Finance & Studio Manager** بالكامل بحيث تكون إعادة استخدام دقيقة لواجهة وتجربة ATHREDU، مع إبقاء Spark تطبيقًا محليًا أولًا، واتجاهه RTL، واحتفاظه بكل منطق المالية والاستوديو وقاعدة بيانات SQLite دون تغيير سلوكي.

المقصود بكلمة «استبدال» هو طبقة العرض والتفاعل فقط: التخطيط، التنقل، المكوّنات، الألوان، الخطوط، الحركة، الحالات، وتصميم الشاشات. لا تحذف أو تعِد كتابة أو تغيّر أسماء جداول Spark أو migrations أو repositories أو services أو domain calculators أو عقود Tauri/SQLite.

## مصادر الحقيقة (اقرأها كاملة قبل التعديل)

1. مشروع الهدف: `D:\Project\Spark Internal\spark-finance-studio-manager`.
2. ابدأ بقراءة `AGENTS.md` ثم `constitution/README.md` وكل ملفات `constitution/*.md` كاملة. هذه هي النسخة المحلية المدققة من دستور ATHREDU، و`constitution/README.md` يحسم التعارضات وقابلية التطبيق على Spark.
3. عند الحاجة لفحص التنفيذ الأصلي، مرجع ATHREDU الوحيد هو `D:\Project\ATHREDU\ATHREDU`. لا تستخدم `Athredu old` ولا أي مجلد داخل `versions`؛ هذه نسخ تاريخية.
4. اقرأ حدود Spark المعتمدة في `docs/ARCHITECTURE.md` وملفات ADR. تعامل مع `docs/DESIGN_SYSTEM.md` كدليل Spark قديم لا يتقدم على الدستور الجديد.

## قواعد لا تقبل التفاوض

- ATHREDU هو المرجع البصري/السلوكي الدقيق: Header-first بلا sidebar، تنقل علوي متجاوب، Dynamic Island للتنبيهات، بطاقات زجاجية، أزرار capsule، PageMotion، Skeleton-first، وDrawer على الجوال وDialog على سطح المكتب.
- انقل أو أعد استخدام مكوّنات ATHREDU المشتركة وtokens اللازمة فقط؛ لا تنقل Supabase أو auth أو PWA أو منطق الطالب/المعلم أو أي نموذج بيانات خاص بـATHREDU.
- لا تستخدم بيانات وهمية أو أرقامًا ثابتة في Spark. كل رقم معروض يأتي من Spark SQLite عبر repositories/services الحالية.
- لا تعدّل SQL migrations أو schema أو domain rules أو repository contracts أو driver API إلا إذا اكتشفت خللًا حقيقيًا، وعندها أوقف العمل وسجّل أثره قبل التغيير.
- لا تدخل اتصالًا شبكيًا في المالية أو الاستوديو أو التشغيل المحلي. Google Drive يبقى ميزة اختيارية ومنفصلة في مرحلة النسخ الاحتياطي.
- حافظ على RTL في كل شاشة Spark وعلى عزل الأرقام/العملة/التواريخ بـ`<bdi>`.
- كل صفحة وبياناتها تملك حالات: skeleton، loading، empty مع إجراء، error قابل لإعادة المحاولة.
- لا تكتفِ بتغيير CSS حول `AppShell` القديم. احذف طبقة واجهة Spark القديمة بعد نقل وظائفها إلى shell ATHREDU الجديد، مع إبقاء ملفات المنطق السليم.

## خطة التنفيذ الملزمة

### 1. جرد وربط قبل البرمجة

أنشئ في `docs/ATHREDU_UX_ADAPTATION.md` جدولًا يربط عناصر ATHREDU بعناصر Spark:

| ATHREDU primitive/flow | Spark replacement | مصدر بيانات Spark | حالة الإنجاز |
|---|---|---|---|

غطِّ: shell، header، profile/menu، navigation، toast/Dynamic Island، modal/drawer، forms، cards، tables/cards mobile، dashboard، clients، finance، contracts، packages/reels، studio، reports، backup/settings.

### 2. طبقة التصميم المشتركة أولًا

انقل/adapt tokens والخطوط والمكوّنات المشتركة المطلوبة إلى Spark، في مجلد واضح مثل `src/ui/athredu/` أو أسماء مكافئة. لا تنسخ الملفات غير المستخدمة. يجب أن تتوافر على الأقل:

- DynamicHeader والتنقل المتجاوب بدون sidebar.
- Button وInput وCard وUserAvatar وSelect وDialog/Drawer وSkeleton وToast/Dynamic Island.
- الخلفية، المسافات، typography، safe areas، والحركة المتوافقة مع ATHREDU.

لا تستبدل `<select>` الأصلي في Spark بواجهة شكلية غير متاحة: استخدم Select ATHREDU فعليًا مع لوحة مفاتيح وARIA.

### 3. استبدال الـshell والتنقل

- استبدل `src/app/AppShell.tsx` بالهيكل header-first المطابق لـATHREDU.
- تنقل Spark يبقى لهذه الأقسام: لوحة التحكم، العملاء، المالية والمصروفات، العقود والاشتراكات، الباقات والريلز، جدول الاستوديو، التقارير، النسخ والضبط.
- صِغ أسماء وأيقونات Spark فقط؛ لا تترك مفردات طالب/مكتبة/دورة/مدرس من ATHREDU.
- على الجوال: قائمة/Drawer كما في ATHREDU، وبطاقات بدل الجداول عند أقل من `lg`، وكل فعل 44px على الأقل.

### 4. تكييف الشاشات دون المساس بالمنطق

أعد بناء كل شاشة Spark من مكوّنات ATHREDU، وربطها بالـviews/services/repositories الموجودة:

1. **Dashboard:** أرقام حية فقط، إجراءات اليوم، حجوزات اليوم، مستحقات، تحصيلات، وتنبيهات تشغيلية.
2. **Clients:** قائمة/بطاقات، بحث، ملف 360، إنشاء/تعديل/أرشفة.
3. **Finance:** دفعات، توزيع الدفعة، أرصدة، مصروفات، وإيصالات.
4. **Contracts:** عقود التسويق، الدفعات الشهرية، الاشتراكات والتجديدات.
5. **Packages/Reels:** الكتالوج، الباقات المباعة، أرصدة الساعات/الريلز، ولوحة الإنتاج.
6. **Studio:** day/week/month UI، إنشاء/تعديل/إلغاء/إكمال، الحجز المتكرر، وعرض التعارض في Dialog/Drawer.
7. **Reports:** تدفق نقدي، دخل بحسب الخدمة، استغلال الاستوديو، التصدير عند توفره.
8. **Backup/Settings:** حالة النسخة اليومية، Backup now، سجل النسخ، استعادة آمنة مع تحذير واضح، وحالة Google Drive الاختيارية. لا تدّعِ أن Drive متصل دون OAuth حقيقي.

### 5. إزالة الواجهة السابقة بأمان

بعد أن يعمل كل مسار على الواجهة الجديدة، احذف فقط ملفات المظهر والمكوّنات غير المستخدمة من Spark. لا تحذف services أو repositories أو tests أو migrations. نفّذ `rg` قبل الحذف لإثبات عدم وجود imports.

## التحقق الإلزامي قبل التسليم

1. شغّل `npm run typecheck` و`npm run lint` و`npm run test` و`npm run build` داخل مشروع Spark، وكلها تمر بلا أخطاء.
2. افتح `http://127.0.0.1:1420` وتحقق يدويًا من الشاشة الرئيسية ومن شاشة واحدة على الأقل لكل قسم أعلاه، على عرض جوال وعرض سطح مكتب.
3. اختبر تدفقًا فعليًا: إنشاء عميل، إنشاء حجز مع منع التعارض، تسجيل دفعة، ثم إعادة تحميل الواجهة والتأكد من بقاء البيانات/عرضها بحسب runtime الحالي.
4. لا تسلّم إذا بقيت أرقام تجريبية أو بطاقات "قريبًا" أو عناصر من ATHREDU لا تخص Spark.
5. حدّث `docs/ATHREDU_UX_ADAPTATION.md` بحالة كل صف واذكر الملفات التي حُذفت أو تم الحفاظ عليها ولماذا.

## صيغة التسليم

أرسل ملخصًا قصيرًا يتضمن: ما تم نقله من ATHREDU، الشاشات المتصلة ببيانات حية، الاختبارات المنفذة ونتائجها، وأي عائق حقيقي (خصوصًا OAuth لـGoogle Drive أو أوامر Tauri/Rust غير المكتملة). لا تقل إن المنتج مكتمل إذا كانت وظيفة التشغيل أو النسخ الاحتياطي أو الاستعادة غير منفذة فعليًا.
